package com.cui.chapter03.p03_03_ThreadLocal;

/**
 * 变量值的共享可以使用public static变量的形式，所有线程都使用同一个public static变量。
 *
 * 类TreadLocal主要解决的就是每个线程绑定自己的值，可以将ThreadLocal类比喻成全局存放数
 * 据的例子，例子中可以存储每个线程的私有数据。
 */
public class Note3 {
}
